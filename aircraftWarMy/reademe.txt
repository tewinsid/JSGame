载入图片，是图片可以通过按键左右移动
最开始事件通过相应按键按下实现图片移动（锯齿）
出现了锯齿状移动问题，
  通过定义按键按下的flag判断是否移动，实现平滑移动
    通过定时器，如果flag为true，执行坐标改变，然后清空canvas重新绘制图像
实现左边界右边界
<script></script>只能这种形式引入js文件，不能简写为<script/>
--------------------零分代码-------------------------------
最外层只能有函数不能有变量，程序应该只有一个入口
把程序改造为只有一个程序入口
  定义入口，在入口里面初始化
  抽象对象，对象的属性和定义
  抽象共有代码
  对象抽象，
  程序只有一个入口
  不断抽象不同的函数做相似的事情
  模块化代码
  抽象英雄
  封装时间，定时器，和绘图
  重点 对按键的时间进行分析
    注册事件，数量太多需要进行抽象
    在初始模式中事件 通过按下和放开分别更新flag事件
    现在需要把事件作为全局变量的一个属性，这样就需要吧flag作为数组存储在全局对象中
    通过一个map存储按键值和事件执行的对应
    在update中遍历事件map，查找对应flag数组，为true，调用事件对应代码（回调）
    需要吧事件的回调函数注册到全局对象中（resgiterAction)
    通过register事件更新flag数组
  把答案中除了已经抽取出的对象的那一部分，拿出来抽取成一个全局对象
  对于重复注册点击事件，通过一个对象保存按键名和对应的处理函数（需注册）
    吧具体的操作延迟到以后进行定义，并使之与当前对象相关联（注册）
    定义map记录未来要注册的时间和处理函数
    定义map记录处理函数执行flag
    暴露注册接口
    在更新方法中遍历actions取得key，查看key对应是否为true，1调用对应处理函数
---------------完成初步抽象---------------------
写程序并不是有工具才用他，而是有需求才去创造工具
